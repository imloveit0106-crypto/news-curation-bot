# News Curation Bot - 技術仕様書

**バージョン**: 1.0.0 (Pro Edition)
**最終更新日**: 2026-01-08
**ステータス**: 完成・稼働中

---

## 目次

1. [プロジェクト概要](#1-プロジェクト概要)
2. [システムアーキテクチャ](#2-システムアーキテクチャ)
3. [技術スタック](#3-技術スタック)
4. [機能仕様](#4-機能仕様)
5. [データモデル](#5-データモデル)
6. [アルゴリズム詳細](#6-アルゴリズム詳細)
7. [CI/CD パイプライン](#7-cicd-パイプライン)
8. [ファイル構成](#8-ファイル構成)
9. [開発環境](#9-開発環境)
10. [運用仕様](#10-運用仕様)
11. [制限事項と今後の拡張性](#11-制限事項と今後の拡張性)

---

## 1. プロジェクト概要

### 1.1 プロジェクト名

**News Curation Bot** (Pro Edition)

### 1.2 目的

AI・世界経済・金融分野の最新ニュースを自動収集し、重要度判定・重複排除を行い、ユーザーに整理された形で提供するニュースキュレーションシステム。

### 1.3 主な特徴

- **多言語対応**: 日本語・英語のニュースソースをサポート
- **重要度判定**: AI・半導体・金融関連キーワードに基づく3段階評価（🔥🔥🔥 高 / 🔥🔥 中 / 低）
- **重複排除**: 履歴管理による既読記事のフィルタリング
- **自動実行**: GitHub Actions による毎日定時実行（JST 8:00）
- **JSON 出力**: フロントエンド連携用のデータファイル生成

### 1.4 対象ユーザー

- テック・金融業界のプロフェッショナル
- AI・半導体トレンドを追う研究者・投資家
- 効率的に情報収集したいビジネスパーソン

---

## 2. システムアーキテクチャ

### 2.1 全体構成

```
┌─────────────────────────────────────────────────────────────┐
│                    GitHub Actions (Scheduler)                │
│                     Cron: 0 23 * * * (UTC)                   │
│                   = JST 8:00 毎日自動実行                     │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│              News Curation Bot (Node.js/TypeScript)          │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  1. RSS Feed 取得（rss-parser）                      │    │
│  │     - Google News RSS (日本語)                       │    │
│  │     - The Verge RSS (英語)                           │    │
│  │     - Reuters via Google News (英語)                │    │
│  └─────────────────────────────────────────────────────┘    │
│                     │                                        │
│                     ▼                                        │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  2. フィルタリング処理                                │    │
│  │     - 除外キーワード判定（恋愛・エンタメ排除）          │    │
│  │     - 重複チェック（history.json との照合）            │    │
│  └─────────────────────────────────────────────────────┘    │
│                     │                                        │
│                     ▼                                        │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  3. 重要度判定                                        │    │
│  │     - 高重要度: NVIDIA, AI, 半導体等                  │    │
│  │     - 中重要度: 金利, 株価, FRB等                     │    │
│  │     - 低重要度: その他                                │    │
│  └─────────────────────────────────────────────────────┘    │
│                     │                                        │
│                     ▼                                        │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  4. ソート & 出力                                     │    │
│  │     - 重要度順にソート                                │    │
│  │     - Console 整形表示                                │    │
│  │     - JSON ファイル出力 (data/news.json)              │    │
│  │     - 履歴更新 (data/history.json)                    │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                     │
                     ▼
          ┌──────────────────────┐
          │  GitHub Actions Cache │
          │  - history.json       │
          └──────────────────────┘
```

### 2.2 データフロー

1. **入力**: Google News RSS, The Verge RSS, Reuters RSS
2. **処理**: パース → フィルタリング → 重要度判定 → 重複排除
3. **出力**: Console表示, JSON ファイル (`data/news.json`, `data/history.json`)

---

## 3. 技術スタック

### 3.1 言語・ランタイム

| 項目 | バージョン | 用途 |
|------|-----------|------|
| **Node.js** | >= 18.0.0 | 実行環境 |
| **TypeScript** | 5.3.0 | 開発言語 |
| **Target** | ES2022 | コンパイルターゲット |

### 3.2 主要ライブラリ

| ライブラリ | バージョン | 用途 |
|-----------|-----------|------|
| `rss-parser` | 3.13.0 | RSS フィード解析 |
| `@types/node` | 20.10.0 | Node.js 型定義 |

### 3.3 UI コンポーネント（フロントエンド連携用）

| ライブラリ | バージョン | 用途 |
|-----------|-----------|------|
| `@radix-ui/react-accordion` | 1.2.12 | アコーディオンUI |
| `@radix-ui/react-dialog` | 1.1.15 | ダイアログUI |
| `@radix-ui/react-dropdown-menu` | 2.1.16 | ドロップダウンメニュー |
| `@radix-ui/react-slot` | 1.2.4 | スロットコンポーネント |
| `framer-motion` | 12.24.11 | アニメーション |
| `lucide-react` | 0.562.0 | アイコン |
| `tailwind-merge` | 3.4.0 | Tailwind CSS クラス管理 |
| `class-variance-authority` | 0.7.1 | バリアント管理 |
| `clsx` | 2.1.1 | クラス名結合 |

### 3.4 開発ツール

| ツール | バージョン | 用途 |
|--------|-----------|------|
| **ESLint** | 8.55.0 | 静的解析 |
| **Prettier** | 3.1.0 | コード整形 |
| **ts-node** | 10.9.2 | 開発時実行 |

### 3.5 CI/CD

| サービス | 用途 |
|----------|------|
| **GitHub Actions** | 自動ビルド・テスト・実行 |
| **GitHub Cache** | 履歴データの永続化 |

---

## 4. 機能仕様

### 4.1 コア機能

#### 4.1.1 RSS フィード取得

**実装**: `fetchNews()` 関数 (src/index.ts:359-396)

- **対応フィード**:
  - Google News (日本語): AI, LLM, 世界経済, 金融
  - The Verge (英語): テック全般
  - Reuters via Google News (英語): AI, NVIDIA, 半導体

- **取得件数制限**:
  - カテゴリ毎: 最大 5 件 (`MAX_ITEMS_PER_CATEGORY`)
  - 総合: 最大 15 件 (`MAX_TOTAL_ITEMS`)

- **エラーハンドリング**:
  - フィード取得失敗時は該当カテゴリをスキップし、警告を表示

#### 4.1.2 重要度判定

**実装**: `judgeImportance()` 関数 (src/index.ts:259-277)

**判定ロジック**:

| 重要度 | キーワード例 | 表示 |
|--------|-------------|------|
| **高** (high) | NVIDIA, AI, 半導体, GPU, OpenAI, ChatGPT, Claude, Anthropic | 🔥🔥🔥 |
| **中** (medium) | 利上げ, FRB, 株価, 為替, 金利, インフレ, GDP | 🔥🔥 |
| **低** (low) | 上記以外 | （表示なし） |

**特徴**:
- 大文字小文字を区別しない (`toLowerCase()` で正規化)
- 複数キーワードマッチ時は最高重要度を適用

#### 4.1.3 重複排除

**実装**: `filterDuplicates()`, `loadHistory()`, `saveHistory()` 関数

**仕組み**:
1. `data/history.json` から過去のタイトルを読み込み
2. 新規記事のタイトルを Set で照合
3. 未登録のみをフィルタリング
4. 処理後、新しいタイトルを履歴に追加
5. 最大 500 件まで保持 (`MAX_HISTORY_ITEMS`)

**履歴ファイル構造**:
```json
{
  "titles": ["記事タイトル1", "記事タイトル2", ...],
  "lastUpdated": "2026-01-08T00:00:00.000Z"
}
```

#### 4.1.4 除外フィルタリング

**実装**: `shouldExclude()` 関数 (src/index.ts:314-317)

**除外キーワード** (`EXCLUDE_KEYWORDS`):
- 日本語: 恋愛, ドラマ, 芸能, アイドル, バラエティ, 映画, 俳優, 女優, デート, 結婚, 熱愛, 交際
- 英語: celebrity, dating, romance, entertainment

**目的**: ビジネス・テック向けニュースに特化するため、エンタメ系を自動除外

#### 4.1.5 JSON 出力（フロントエンド連携）

**実装**: `saveNewsToJSON()` 関数 (src/index.ts:449-467)

**出力先**: `data/news.json`

**データ構造**:
```json
{
  "lastUpdated": "2026-01-08T00:00:00.000Z",
  "totalCount": 10,
  "items": [
    {
      "id": "1736294400000-AI・LLM-0",
      "title": "2026年、AIの未来を揺さぶる6つの予測",
      "url": "https://news.google.com/...",
      "publishedAt": "1月5日 07:00",
      "source": "WIRED.jp",
      "importance": "high",
      "category": "AI・LLM"
    }
  ]
}
```

### 4.2 ユーティリティ機能

#### 4.2.1 日付整形

**実装**: `formatDate()` 関数 (src/index.ts:322-335)

**出力形式**: `1月5日 07:00` (日本語ロケール)

#### 4.2.2 ソース名抽出

**実装**: `extractSource()` 関数 (src/index.ts:340-343)

**ロジック**: タイトル末尾の `- ソース名` 形式からメディア名を抽出

#### 4.2.3 タイトルクリーニング

**実装**: `cleanTitle()` 関数 (src/index.ts:348-350)

**ロジック**: タイトルからソース部分を削除

---

## 5. データモデル

### 5.1 型定義

#### NewsItem インターフェース

```typescript
interface NewsItem {
  id: string;              // 一意のID（タイムスタンプ + カテゴリ + インデックス）
  title: string;           // 記事タイトル（ソース名除去済み）
  url: string;             // 記事URL
  publishedAt: string;     // 公開日時（整形済み）
  source: string;          // メディア名
  importance: ImportanceLevel; // 重要度（high/medium/low）
  category: string;        // カテゴリ名
}
```

#### FeedConfig インターフェース

```typescript
interface FeedConfig {
  category: string;        // カテゴリ名
  url: string;             // RSS フィード URL
  lang: 'ja' | 'en';       // 言語
}
```

#### ImportanceKeywords インターフェース

```typescript
interface ImportanceKeywords {
  high: string[];          // 高重要度キーワード配列
  medium: string[];        // 中重要度キーワード配列
}
```

---

## 6. アルゴリズム詳細

### 6.1 重要度判定アルゴリズム

```typescript
function judgeImportance(title: string): ImportanceLevel {
  const lowerTitle = title.toLowerCase();

  // Step 1: 高重要度キーワードチェック
  for (const keyword of IMPORTANCE_KEYWORDS.high) {
    if (lowerTitle.includes(keyword.toLowerCase())) {
      return 'high';
    }
  }

  // Step 2: 中重要度キーワードチェック
  for (const keyword of IMPORTANCE_KEYWORDS.medium) {
    if (lowerTitle.includes(keyword.toLowerCase())) {
      return 'medium';
    }
  }

  // Step 3: デフォルトは低重要度
  return 'low';
}
```

**計算量**: O(n × m) (n = キーワード数, m = タイトル長)

### 6.2 ソートアルゴリズム

**実装**: `sortByImportance()` 関数 (src/index.ts:401-408)

```typescript
function sortByImportance(items: NewsItem[]): NewsItem[] {
  return [...items].sort((a, b) => {
    // 重要度を数値化（high=3, medium=2, low=1）
    const diff = importanceToNumber(b.importance) - importanceToNumber(a.importance);
    if (diff !== 0) return diff;
    // 同じ重要度なら維持（安定ソート）
    return 0;
  });
}
```

**ソート順**:
1. 重要度降順（高 → 中 → 低）
2. 同一重要度内では取得順を維持

---

## 7. CI/CD パイプライン

### 7.1 CI ワークフロー (`.github/workflows/ci.yml`)

**トリガー**:
- `push` to `main` / `master`
- `pull_request` to `main` / `master`

**ジョブ**:

```yaml
jobs:
  build-and-check:
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    steps:
      - Checkout
      - Setup Node.js
      - npm ci
      - npm run format:check  # Prettier チェック
      - npm run lint          # ESLint チェック
      - npm run build         # TypeScript コンパイル
```

### 7.2 Daily News ワークフロー (`.github/workflows/daily-news.yml`)

**トリガー**:
- **定時実行**: Cron `0 23 * * *` (UTC) = JST 8:00
- **手動実行**: `workflow_dispatch`
- **PR**: `pull_request` (ステータスチェック用)

**主要ステップ**:

```yaml
steps:
  1. Checkout
  2. Setup Node.js 20
  3. npm ci
  4. npm run build
  5. Restore history cache (actions/cache@v4)
  6. mkdir -p data
  7. npm run start  # ニュース取得実行
  8. Save history cache
  9. Summary 生成
```

**キャッシュ戦略**:
- **キー**: `news-history-{branch}`
- **パス**: `data/history.json`
- **目的**: 重複排除の履歴を GitHub Actions 実行間で永続化

---

## 8. ファイル構成

```
news-curation-bot/
├── .github/
│   └── workflows/
│       ├── ci.yml                # CI ワークフロー
│       └── daily-news.yml        # Daily ニュース取得ワークフロー
├── src/
│   └── index.ts                  # メインロジック（519行）
├── dist/                         # TypeScript コンパイル成果物
│   └── index.js
├── data/                         # 実行時生成ディレクトリ
│   ├── history.json              # 重複排除用履歴
│   └── news.json                 # フロントエンド用ニュースデータ
├── package.json                  # 依存関係・スクリプト定義
├── package-lock.json
├── tsconfig.json                 # TypeScript 設定
├── .eslintrc.json                # ESLint 設定
├── .prettierrc                   # Prettier 設定
├── .gitignore
└── README.md                     # ユーザー向けドキュメント
```

---

## 9. 開発環境

### 9.1 TypeScript 設定 (`tsconfig.json`)

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "commonjs",
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "declaration": true,
    "sourceMap": true
  }
}
```

### 9.2 ESLint 設定 (`.eslintrc.json`)

**主要ルール**:
- `@typescript-eslint/explicit-function-return-type`: warn
- `@typescript-eslint/no-unused-vars`: error
- `@typescript-eslint/no-explicit-any`: warn
- `no-console`: warn（ただし実装では許容）

### 9.3 Prettier 設定 (`.prettierrc`)

```json
{
  "semi": true,
  "singleQuote": true,
  "printWidth": 100,
  "tabWidth": 2,
  "trailingComma": "es5"
}
```

### 9.4 npm スクリプト

| コマンド | 説明 |
|---------|------|
| `npm run build` | TypeScript → JavaScript コンパイル |
| `npm run start` | ビルド済みコード実行 |
| `npm run dev` | ts-node で直接実行（開発用） |
| `npm run lint` | ESLint チェック |
| `npm run lint:fix` | ESLint 自動修正 |
| `npm run format` | Prettier 整形 |
| `npm run format:check` | Prettier チェックのみ |

---

## 10. 運用仕様

### 10.1 自動実行スケジュール

- **頻度**: 毎日 1 回
- **実行時刻**: JST 8:00 (UTC 23:00 前日)
- **実行環境**: GitHub Actions (`ubuntu-latest`)

### 10.2 ログ確認方法

1. リポジトリの **Actions** タブにアクセス
2. **Daily News Curation** ワークフローを選択
3. 最新の実行をクリック
4. **fetch-news** ジョブを展開
5. "Fetch latest news" ステップでニュース一覧を確認

### 10.3 手動実行

GitHub Actions の **Run workflow** ボタンから即座に実行可能

### 10.4 データ永続化

- **履歴データ**: GitHub Actions Cache に保存（ブランチ毎）
- **最大保持件数**: 500 件（古いものから削除）

---

## 11. 制限事項と今後の拡張性

### 11.1 現在の制限事項

1. **言語**: 日本語・英語のみ対応
2. **出力形式**: Console + JSON のみ
3. **通知機能**: なし（ログ確認が必要）
4. **カスタマイズ**: コード修正が必要（UI なし）
5. **データ分析**: 統計・トレンド分析機能なし

### 11.2 拡張可能性

#### Phase 2 候補機能

- [ ] **Web UI**: React フロントエンドの実装（`package.json` に UI ライブラリは既存）
- [ ] **通知機能**: Slack, Discord, Email 連携
- [ ] **多言語対応**: 中国語、韓国語ソース追加
- [ ] **AI 要約**: OpenAI API でニュース要約生成
- [ ] **データ分析**: トレンドグラフ、キーワード頻度分析
- [ ] **カスタムフィード**: ユーザー毎のキーワード設定

#### 技術的改善

- [ ] **データベース**: 履歴を SQLite/PostgreSQL で管理
- [ ] **API サーバー**: Express.js で REST API 提供
- [ ] **テスト**: Jest によるユニット/E2Eテスト
- [ ] **監視**: Sentry でエラートラッキング

---

## 付録A: 主要定数一覧

| 定数名 | 値 | 説明 |
|--------|---|------|
| `MAX_ITEMS_PER_CATEGORY` | 5 | カテゴリ毎の取得上限 |
| `MAX_TOTAL_ITEMS` | 15 | 総合取得上限 |
| `MAX_HISTORY_ITEMS` | 500 | 履歴保持上限 |
| `HISTORY_FILE` | `data/history.json` | 履歴ファイルパス |
| `NEWS_OUTPUT_FILE` | `data/news.json` | 出力ファイルパス |

---

## 付録B: RSS フィード一覧

| カテゴリ | URL | 言語 |
|---------|-----|------|
| AI・LLM | `https://news.google.com/rss/search?q=AI+OR+LLM+OR+OpenAI+OR+NVIDIA+OR+ChatGPT+OR+Claude&hl=ja&gl=JP&ceid=JP:ja` | 日本語 |
| 世界経済 | `https://news.google.com/rss/search?q=世界経済+OR+グローバル経済+OR+GDP&hl=ja&gl=JP&ceid=JP:ja` | 日本語 |
| 金融速報 | `https://news.google.com/rss/search?q=金融+OR+株式市場+OR+為替+OR+日銀&hl=ja&gl=JP&ceid=JP:ja` | 日本語 |
| Tech (Global) | `https://www.theverge.com/rss/index.xml` | 英語 |
| Business (Reuters) | `https://news.google.com/rss/search?q=site:reuters.com+AI+OR+NVIDIA+OR+semiconductor&hl=en-US&gl=US&ceid=US:en` | 英語 |

---

## 付録C: 重要度キーワード完全リスト

### 高重要度（high）

**日本語**: NVIDIA, エヌビディア, AI, 人工知能, 半導体, GPU, OpenAI, ChatGPT, Claude, Anthropic, Google AI, Gemini, LLM, 大規模言語モデル

**英語**: artificial intelligence, semiconductor, chip, neural network, machine learning, deep learning, GPT, transformer

### 中重要度（medium）

**日本語**: 利上げ, 利下げ, 日銀, 日本銀行, FRB, 株価, 株式, 為替, 円安, 円高, ドル, 金利, インフレ, GDP

**英語**: interest rate, federal reserve, stock market, inflation, economy, earnings, investment, market cap

---

**ドキュメント作成者**: Claude (Anthropic AI)
**承認ステータス**: 初版
**次回レビュー予定**: 機能追加時
